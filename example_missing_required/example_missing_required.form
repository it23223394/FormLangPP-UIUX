form ErrorTest {
    section MissingRequired {
        field email: email;
        field poem: textarea;
    }
    
    validate {
        if (age < 18) {
            error "Too young.";
        }
    }
}
